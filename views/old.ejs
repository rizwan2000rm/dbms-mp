<%- include('partials/header') %>

<!-- POSTS -->
<div class="container mx-auto my-3">
  <h3>Finding 5 oldest users</h3>
  <div class="hashtags">
    <% rows.forEach((row) => { %>
    <button class="btn btn-info text-white"><%= row.username %></button>
    <% }); %>
  </div>
</div>

<%- include('partials/footer') %>
