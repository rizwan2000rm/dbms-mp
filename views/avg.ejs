<%- include('partials/header') %>

<!-- POSTS -->
<div class="container mx-auto my-3">
  <h3>Calculate average number of photos per user</h3>
  <div class="hashtags">
    <% rows.forEach((row) => { %>
    <button class="btn btn-info text-white"><%= row.avg %></button>
    <% }); %>
  </div>
</div>

<%- include('partials/footer') %>
