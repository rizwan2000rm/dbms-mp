<%- include('partials/header') %>

<!-- POSTS -->
<div class="container mx-auto"
  <div class="container my-3">
    <h3>Popular Hastags</h3>
    <div class="hashtags">
      <% rows.forEach((row) => { %>
      <button class="btn btn-info text-white"><%= row.tag_name %></button>
      <% }); %>
    </div>
  </div>
</div>

<%- include('partials/footer') %>